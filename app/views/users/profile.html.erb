<% if @user %>
  <h1><%= @user.name %></h1>

<section>
  <h2>Sites</h2>    
  <% @user.tenants.each do |tenant|%>
    <h3><%= tenant.name %></h2>
    <p><%= tenant.subdomain %></p>
  <% end %>

</section>
<% end %>

<%= link_to 'Back', root_path %>